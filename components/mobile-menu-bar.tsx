interface MobileMenuBarProps {
  isSidebarShowing: boolean;
  setIsSidebarShowing: any;
}


const MobileMenuBar = ({ isSidebarShowing, setIsSidebarShowing}: MobileMenuBarProps) => {
  return (
    <div>

    </div>
  );
};

export default MobileMenuBar;
